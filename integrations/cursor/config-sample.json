{
  "_comment": "Cursor: Settings → MCP → Add new MCP server. Paste the server block below. Cursor will surface Sureguard's tools to the chat agent.",
  "mcpServers": {
    "sureguard": {
      "command": "uvx",
      "args": ["sureguard-code-scanner"]
    }
  }
}
